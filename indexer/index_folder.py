"""Folder indexer entry point. See indexer/README.md and spec.md §6.1."""


def index_folder(path: str) -> list[dict]:
    """Walk *path* (top-level) and return FileRecord dicts.

    Raises:
        NotImplementedError: stub until owner implements (Task 2 / 0:30 fixtures).
    """
    raise NotImplementedError("See indexer/README.md — stub pending fixture-shaped return.")
