# Taglish uses TL template

When `language_mode` is `taglish`, `speak` uses the Tagalog template text and `routing.taglish` voice_id (often the same id as `tl`). Matches §8.2 routing shape without a third phrase string.
